# ============================================================
#  电机振动故障检测 —— Git 忽略规则
#  保存编码：UTF-8（不带 BOM）
#  改完用 git check-ignore -v <路径> 验证是否生效
# ============================================================

# ---------- 1. 项目内的 Python 环境（约 160 MB，绝不入库） ----------
.conda/
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# ---------- 2. Keil MDK 编译产物（每编译一次全变） ----------
Output/
OBJ/
Objects/
Listings/
DebugConfig/
RTE/
*.o
*.crf
*.d
*.axf
*.elf
*.hex
*.bin
*.map
*.htm
*.dep
*.lnp
*.lst
*.plg
*.sct
*.tra
*.iex
*.saved_uv4
*.bak
*.Bak

# ---------- 3. 只属于你本机的 Keil 配置（含用户名 liyunlong） ----------
*.uvguix.*
*.uvopt
*.uvoptx
JLinkLog.txt
JLinkSettings.ini

# ---------- 4. 重复副本 / 第三方资料包（本机保留，不入库） ----------
GY521mpu-6050资料/

# ---------- 5. 原始采集数据（整目录不入库） ----------
#  结档时想放少量样例进仓库，用：git add -f 04_数据集/raw/xxx.csv
04_数据集/raw/

# ---------- 6. 系统 / 编辑器临时文件 ----------
Thumbs.db
desktop.ini
.DS_Store
*~

# ---------- 7. VS Code / Keil Assistant 的本机文件与日志 ----------
#  *.log 无斜杠 → 匹配任意层级的日志；带斜杠会被钉死在仓库根，匹配不到嵌套目录
*.log
*.log.lock
#  嵌套在 Keil 工程旁的 .vscode 是 Keil Assistant 自动生成的（含 MDK 安装路径），只忽略这一处，保留仓库根的 .vscode
00_工程文件/**/.vscode/

# ---------- 8. 仅本地保存，不上传云端 ----------
01_文档/电机振动故障检测_项目规划.md